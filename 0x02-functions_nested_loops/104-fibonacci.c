#include <stdio.h>
/**
 * main - prints first 98 fibonacci series
 * Return: Always 0
 */
int main(void)
{
	int i, num1 = 0, num2 = 1, sum;

	for (i = 0; i < 98; i++)
	{
		sum = num1 + num2;
		printf("%d, ", sum);
		num1 = num2;
		numb2 = su;
	}
	printf("\n");

	return (0);
}
