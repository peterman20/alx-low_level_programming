#include <stdlib.h>
#include <unistd.h>
#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @n: the integer to be checked
 * Return: return 0
 */
int _abs(int n)
{
	return (abs(n));
	return (0);
}
